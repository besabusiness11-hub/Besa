<!DOCTYPE html>
<html lang="it">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>BESA - Sito</title>
<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  /* Schermata logo iniziale */
  #logo-screen {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 1000;
    transition: transform 0.8s ease-out, opacity 0.8s ease-out;
  }

  /* Logo senza sfondo quadrato */
  #logo {
    font-family: 'Arial', sans-serif;
    font-size: 8rem;
    font-weight: bold;
    color: #4a69bd;
    text-shadow: 0 4px 8px rgba(0,0,0,0.1);
    letter-spacing: 0.1em;
  }

  /* Stato nascosto - slide up */
  #logo-screen.hidden {
    transform: translateY(-100%);
    opacity: 0;
  }

  /* Contenuto principale */
  #main-content {
    height: 200vh; /* Altezza maggiore per permettere scroll */
    background: white;
  }

  #homepage {
    padding: 2rem;
    margin-top: 100vh; /* Inizia dopo la prima schermata */
  }

  /* Indicatore scroll */
  .scroll-indicator {
    position: absolute;
    bottom: 30px;
    left: 50%;
    transform: translateX(-50%);
    color: #4a69bd;
    font-size: 0.9rem;
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% { transform: translateX(-50%) translateY(0); }
    40% { transform: translateX(-50%) translateY(-10px); }
    60% { transform: translateX(-50%) translateY(-5px); }
  }
</style>
</head>
<body>

<div id="logo-screen">
  <div id="logo">BESA</div>
  <div class="scroll-indicator">
    ↓ Scorri per continuare
  </div>
</div>

<div id="main-content">
  <div id="homepage">
    <h1>Benvenuto su BESA</h1>
    <p>Qui inizia il tuo sito...</p>
    <!-- Il resto del tuo contenuto -->
  </div>
</div>

<script>
  window.addEventListener('scroll', () => {
    const scrollY = window.scrollY;
    const logoScreen = document.getElementById('logo-screen');
    
    // Quando si scrolla per più di 50px, nascondi il logo
    if (scrollY > 50) {
      logoScreen.classList.add('hidden');
    } else {
      logoScreen.classList.remove('hidden');
    }
  });
</script>

</body>
</html>
